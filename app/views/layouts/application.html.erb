<!DOCTYPE html>
<html>
  <head>
    <title>Charamite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!-- Alpine.js 読み込み -->
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
  </head>

  <body class="min-h-screen flex flex-col">

    <!-- ヘッダー：上部に固定されず通常配置 -->
    <header class="bg-gradient-to-r from-cyan-300 to-purple-300 w-full">
      <%= render "shared/header" %>
    </header>

    <!-- メイン：中央に白背景の内容 -->
    <main class="flex-grow bg-white flex items-center justify-center px-4 py-10">
      <div class="w-full max-w-xl">
        <%= yield %>
      </div>
    </main>

    <!-- フッター：下部に配置 -->
    <footer class="bg-gradient-to-r from-cyan-300 to-purple-300 w-full">
      <%= render "shared/footer" %>
    </footer>

  </body>
</html>
